============
Installation
============

Install the package with pip::

    $ pip install read-the-docs-template

==============
R Installation
==============

Install the package with apt::

    $ sudo apt install R
